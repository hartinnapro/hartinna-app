# HTML — always revalidate so browsers pick up new deployments immediately
/*
  Cache-Control: no-cache, no-store, must-revalidate

# Hashed JS/CSS assets — cache forever (filename changes on every deploy)
/assets/*
  Cache-Control: public, max-age=31536000, immutable
